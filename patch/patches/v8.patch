diff --git a/src/v8.gyp b/src/v8.gyp
index 70a1c6f..1277af1 100644
--- src/v8.gyp
+++ src/v8.gyp
@@ -2600,11 +2600,6 @@
             '<(icu_gyp_path):icuuc',
           ]
         }],
-        ['want_separate_host_toolset==1', {
-          'toolsets': ['host'],
-        }, {
-          'toolsets': ['target'],
-        }],
       ],
     },
     {
diff --git include/libplatform/libplatform.h include/libplatform/libplatform.h
index 756ccb1..ea481e6 100644
--- include/libplatform/libplatform.h
+++ include/libplatform/libplatform.h
@@ -33,7 +33,7 @@ enum class MessageLoopBehavior : bool {
  * If |tracing_controller| is nullptr, the default platform will create a
  * v8::platform::TracingController instance and use it.
  */
-V8_PLATFORM_EXPORT v8::Platform* CreateDefaultPlatform(
+V8_EXPORT2 v8::Platform* CreateDefaultPlatform(
     int thread_pool_size = 0,
     IdleTaskSupport idle_task_support = IdleTaskSupport::kDisabled,
     InProcessStackDumping in_process_stack_dumping =
